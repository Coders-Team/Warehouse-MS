
@using Warehouse_MS.Models.DTO
@model WarehouseDto
@{
    Layout = "_Layout";
}

@*<div>
        <h4>@Model.Name</h4>
        <hr />
        @if (Model.Storages.Count == 0)
                {
            <p>No storage found in this Warehouse</p>
        }
        else
        {
        <table border=1 class="table">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Name
                </th>

                <th>
                    SizeInUnit
                </th>
                <th>
                    LocationInWarehouse
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Storages) {
         <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                 <td>
                    @Html.DisplayFor(modelItem => item.SizeInUnit)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LocationInWarehouse)
                </td>

         </tr>
            }
        </tbody>
    </table>
    }
    </div>*@

@if (Model.Storages.Count == 0)
{
    <p>No storage found in this Warehouse</p>
}
else
{

    <div class="container-xxl flex-grow-1 container-p-y">
        <div class="row row-cols-1 row-cols-md-3 g-4 mb-5" style="height:20rem;">
            @foreach (var item in Model.Storages)
            {
                <div class="col">
                    <div class="col">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title">@item.Name</h5>
                                <h6 class="card-subtitle text-muted">@item.LocationInWarehouse</h6>
                            </div>
                            <div class="card-body">
                                <p class="card-text">@item.Description</p>
                                <p class="card-text">@item.SizeInUnit</p>



                            </div>
                        </div>
                    </div>
                </div>

            }
        </div>
    </div>
 } 
